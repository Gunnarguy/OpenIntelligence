# Local Benchmark Fixtures

Put private benchmark documents here when running local RAG validation.

Files in this folder are ignored by git so private manuals, policies, PDFs,
images, and exports do not get committed accidentally. The sample manifest uses
placeholder names from this folder; missing files are reported as skipped cases.

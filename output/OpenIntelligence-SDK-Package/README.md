# OpenIntelligence Engine Packet

This folder supports the current source-first commercial story for the engine.

If you only want the shortest path, open `START_HERE.md`.

Read in this order:

1. `START_HERE.md`
2. `PACKAGE_SUMMARY.md`
3. `INSTALL.md`
4. `API.md`

This packet supports two lanes:

- a source-SDK diligence lane inside the private engine repo
- a packet-local XCFramework evaluation lane for same-toolchain testing

Use this folder for:

- design-partner diligence
- private source SDK licensing conversations
- founder-led demos and same-toolchain import testing

Do not describe it as:

- a sealed stable binary SDK
- a toolchain-agnostic handoff
- a docs-only no-guidance self-serve SDK

If the public release line changes, rebuild this packet from current source before sending it externally.

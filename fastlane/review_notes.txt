No login required. The app works entirely offline.

VERSION 1.2.0 — WHAT'S NEW SINCE v1.1 (Feb 16, 2026):

NEW FEATURE — RICH MARKDOWN RESPONSE RENDERING:
- LLM responses now render with full formatting: headers, bullets, numbered lists, bold text, code blocks, block quotes
- Previously all responses displayed as a single unformatted paragraph
- Full block-level markdown parser replaces the inline-only renderer
- Inline normalization preprocessor splits Apple FM single-line output into proper blocks (6 regex patterns)
- All LLM synthesis prompts updated with formatting instructions
- 7 response-cleaning functions audited to preserve markdown structure

NEW FEATURE — DEVICE-OPTIMIZED PERFORMANCE ENGINE:
- 3-tier Metal GPU shaders (threadgroup/SIMD4/scalar) adapt to corpus size
- Device-specific OCR concurrency (A19 Pro: 8 ops/1ms, A18 Pro: 6/2ms, A17 Pro: 4/3ms)
- Concurrent cross-encoder reranking with pre-tokenized bulk memory writes
- GPU embedding ingestion mode frees Neural Engine for OCR
- 5-candidate OCR evaluates more alternatives for ambiguous text

STABILITY FIX:
- Fixed crash in MMR diversification (array index out of bounds when GPU matrix returned edge-case results)

PREVIOUS FEATURES (from v1.1):
- Motherboard HUD: Real-time Apple Silicon X-ray overlay
- Universal Retrieval: 8 needle-in-haystack accuracy fixes

TO TEST RICH MARKDOWN RENDERING:
1. Launch the app and import any PDF or text file
2. Wait for processing to complete
3. Ask a question about the document
4. Verify the response shows formatted headers (###), bullet lists (-), and bold text (**)
5. Compare with previous versions where responses were a single paragraph

TO TEST THE MOTHERBOARD HUD:
1. Go to Settings → Telemetry section
2. Toggle "Motherboard HUD" on
3. Return to Chat tab
4. The X-ray overlay appears showing real chip positions
5. Ask a question — watch the SoC and Neural Engine indicators pulse during processing
6. Toggle off from Settings when done

SAMPLE TEST: Import any PDF from Files. A simple Wikipedia article export works well. Ask questions like "What is the main topic?" or "Summarize the key points."

SUBSCRIPTION TESTING: The Free tier allows 3 documents. Pro features unlock with subscription purchase.

PRIVACY FEATURES: All processing runs on-device by default. The app uses iOS Foundation Models which may use Private Cloud Compute for complex queries — this is handled automatically by iOS with zero data retention.

NO DEMO CREDENTIALS NEEDED — the app does not require sign-in or account creation.

NO DEMO CREDENTIALS NEEDED — the app does not require sign-in or account creation.

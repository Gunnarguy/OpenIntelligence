No login required. The app works entirely offline.

VERSION 1.1.0 — WHAT'S NEW SINCE v1.0 (Feb 4, 2026):

NEW FEATURE — MOTHERBOARD HUD:
- Real-time Apple Silicon X-ray overlay on the chat screen
- Shows actual chip positions (SoC, NAND, DRAM, modem, PMIC, WiFi, Taptic Engine) verified from iFixit teardown images
- Live CPU, GPU, Neural Engine, memory, thermal, and battery telemetry
- Device-specific layouts for iPhone 15 Pro through iPhone 17 Pro series
- Ultra-subtle ghost outlines that pulse with activity
- Toggle on/off from Settings → Telemetry section

RETRIEVAL IMPROVEMENTS (8 fixes):
- BM25 lexical search now always contributes to hybrid results
- HyDE embedding blended 70/30 with query embedding (preserves user terminology)
- Dynamic synonym generation from document co-occurrence data
- Adaptive cross-encoder candidate pool scaling for large document collections
- Sentence-level fallback when LLM compression fails
- Rare corpus terms preserved in query expansion
- Year/integer exemption in numeric verification (no false positives on dates)
- Proportional RRF fusion weighting based on hit counts

TO TEST THE RAG FEATURES:
1. Launch the app and tap "Add Document" (+ button)
2. Import any PDF or text file from Files app
3. Wait for "Processing complete" notification (~5-10 seconds)
4. Type a question about the document in the chat
5. The app will retrieve relevant passages and generate a cited answer

TO TEST THE MOTHERBOARD HUD:
1. Go to Settings → Telemetry section
2. Toggle "Motherboard HUD" on
3. Return to Chat tab
4. The X-ray overlay appears showing real chip positions
5. Ask a question — watch the SoC and Neural Engine indicators pulse during processing
6. Toggle off from Settings when done

SAMPLE TEST: Import any PDF from Files. A simple Wikipedia article export works well. Ask questions like "What is the main topic?" or "Summarize the key points."

SUBSCRIPTION TESTING: The Free tier allows 3 documents. Pro features unlock with subscription purchase.

PRIVACY FEATURES: All processing runs on-device by default. The app uses iOS Foundation Models which may use Private Cloud Compute for complex queries — this is handled automatically by iOS with zero data retention.

NO DEMO CREDENTIALS NEEDED — the app does not require sign-in or account creation.

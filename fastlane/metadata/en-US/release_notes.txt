Version 2.0.1

SAFETY HARDENING
- 37 force-unwrap crash sites eliminated across 26 files
- 10 fatalError() calls replaced with graceful fallbacks
- 1,278 lines of dead code removed (3 unused files + commented-out class)
- 3 silent Vision error handlers now log properly

BUG FIXES
- Fixed cross-container chat bleed (queries no longer leak between libraries)
- Fixed undismissable alert dialogs in Documents and Cached Docs views
- Fixed empty Insights sheet (Show All Insights now displays content)
- Fixed StoreKit stream continuation crash (IUO replaced with AsyncStream)
- Fixed ContainerService initialization error
- Removed stub System Status and Developer categories from Settings

ONBOARDING POLISH
- Haptic feedback on all 6 onboarding interactions
- VoiceOver labels for pipeline progress, ingestion rows, and stage badges
- Analytics now distinguishes completed vs skipped onboarding
- Fixed misleading "tap to retry" error message
- Added microphone permission for voice input

ONBOARDING REWRITE
- Complete rebuild: 2-page flow with live pipeline theater
- Compact phase capsules (Extract → Chunk → Embed → Index)
- Real-time metrics dashboard (words, chunks, vectors, time)
- Fixed-height streaming log ticker with fade gradient
- Retry button when sample import fails
- Reduce motion and VoiceOver accessibility support
- Swift 6 concurrency migration + dead code cleanup

EDUCATIONAL SAMPLE DOCS
- 3 built-in docs teach you how the app works: Pricing, RAG Architecture, Apple Intelligence & PCC
- Sample imports bypass free tier limit so onboarding always succeeds

SEARCH & RETRIEVAL
- BM25Scorer refactored from class to struct for thread safety
- Image Playground concept extraction uses zero-shot prompt (removed hardcoded Mustang bias)

SUGGESTED QUESTIONS
- Fixed prompt contamination causing off-topic suggestions (car questions on unrelated docs)
- Now generates 4 grounded questions per library from actual document content
- Stale questions cleared instantly when switching libraries
- Natural conversational tone — suggestions sound like questions a person would actually ask
- Fixed race condition where switching libraries fast could show stale suggestions from another library

CONTAINER ISOLATION
- Entity index, full-text search, and suggested questions fully scoped per library
- Document and library deletion properly cleans all cross-references

AI HUB RESULT SHEET
- Transform results (Key Facts, Step-by-Step, etc.) now render with full markdown formatting
- New Share button alongside Copy and Insert in Chat
- Sheet starts at half-height, draggable to full for longer results

ANTI-HALLUCINATION
- LLM can now acknowledge when documents don't cover the question (was forced to fabricate)
- Topical mismatch detection: Evidence-First mode activates when query keywords don't match retrieved chunks

Includes all v2.0 features: 29-step RAG pipeline, AI Hub transforms, 3-tier Metal GPU search, device-specific OCR, rich markdown rendering, 102 services.

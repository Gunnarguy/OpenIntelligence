# OpenIntelligence Adjacent Component Blast Radius Map

## Current Audit Phase (Read-Only)
*   **Target Files:** None (0 production files modified).
*   **Target Symbols:** None.
*   **Direct Callers:** None.
*   **Actor Boundaries:** Protected.
*   **Persisted Models:** Protected.
*   **Database Tables:** Protected.
*   **Sync Paths:** Protected.
*   **Risk Mitigation:** The blast radius of the current audit phase is strictly isolated to read-only inquiries and local control file generation in the `.agent/` folder.

## Analysis of PR #54 (Proposed Change Blast Radius)
If PR #54 were to be implemented, its blast radius would be:
*   **Target File:** `OpenIntelligence/Services/AIPlatform/AppleFoundationModels/FoundationModelSessionFactory.swift`
*   **Target Symbol:** `FoundationModelSessionFactory` initialization flow
*   **Direct Callers:** `LLMService`, `RAGService`
*   **Apple API Dependency:** `SystemLanguageModel.advanced` (Compiler-gated via `#if compiler(>=6.4)`)
*   **Consequences of SDK Missing Symbol:** Compiling with Xcode 27 / Swift 6.4 fails due to missing symbol `advanced` on `SystemLanguageModel`, preventing compilation of the entire `OpenIntelligence` target.

## Analysis of PR #63 (Proposed Change Blast Radius)
If PR #63 were to be implemented, its blast radius would be:
*   **Target File:** `OpenIntelligence/Services/RAG/Retrieval/HybridSearchService.swift`
*   **Target Symbol:** `HybridSearchService.search(...)`
*   **Direct Callers:** `RAGService`, `IterativeRetrievalService`
*   **Blast Radius Components:** All knowledge retrieval pipelines (including cascade searches, retry retrievals, topic classification, and iterative RAG expansion loops).
*   **Consequences of Concurrency and Sizing Policy Changes:**
    1. If `lexicalRecallLimit` does not scale up under sparse vector matches, adjacent services like `RAGService` will receive fewer fallback candidate chunks, decreasing final RAG synthesis quality.
    2. Cancellation of either the database query or the vector index query propagates to the entire hybrid search, causing failure cascades in orchestration layers.

## Analysis of PR #53 (Proposed Change Blast Radius)
If PR #53 were to be implemented, its blast radius would be:
*   **Target File:** `OpenIntelligence/Services/Query/Enhancement/QueryEnhancementService.swift`
*   **Target Symbol:** `QueryEnhancementService.classifyAnswerIntent(_:)`
*   **Direct Callers:** `QueryProfileService`, `SuggestedQuestionsService`, `RAGService`
*   **Blast Radius Components:** Query profiling, suggested question generation UI, and RAG answer orchestration routing.
*   **Consequences of Intent Classification Changes:** If classification is corrected to prevent false lookups (e.g. "askew" incorrectly triggering lookup route), it ensures complex questions route to multi-hop `.investigate` flows rather than short-circuited fact retrievals, protecting RAG answer completeness.

## Analysis of PR #56 (Proposed Change Blast Radius)
If PR #56 were to be implemented, its blast radius would be:
*   **Target File:** `OpenIntelligence/Services/Query/Rewriting/HyDEService.swift`
*   **Target Symbol:** `HyDEService.generateHyDEQuery(for:containerId:config:)`
*   **Direct Callers:** `RAGService`
*   **Blast Radius Components:** Knowledge retrieval pipeline (HyDE-enabled semantic search).
*   **Consequences of Separator Changes:** Changing the query-hypothetical text separator directly alters the embedding vector generated by the embedding model. This shifts semantic distance calculations in the vector database, impacting candidate pool ranks and document overlap outcomes.

## Analysis of PR #57 (Proposed Change Blast Radius)
If PR #57 were to be implemented, its blast radius would be:
*   **Target File:** `OpenIntelligence/Services/RAG/Extraction/ExtractiveQAService.swift`
*   **Target Symbol:** `HeuristicExtractiveQAService` scoring and span extraction methods.
*   **Direct Callers:** `ExtractiveQAServiceFactory`
*   **Blast Radius Components:** Sentence-scoring and answer span extraction layers.
*   **Consequences of Regex and Range Changes:** Eliminates regex recompilation latency during text segment scoring, prevents indexing crashes on UTF-16 code unit boundary mismatches (e.g., when documents contain emojis or multi-byte glyphs), and ensures build compatibility across diverse toolchain compiler versions.

## Cluster-Level Blast Radius for Proposed Implementation (Phase A completion, 2026-07-13)

### Cluster T — Test-target restoration (gate for everything else)
*   **Target files (Tier 1 — named authorization required):** `OpenIntelligence.xcodeproj/project.pbxproj`, both `.xcscheme` files, possibly `Package.swift`.
*   **Blast radius:** build system only; no runtime behavior. NOTE: the working tree already contains an uncommitted version of exactly this change (AUTH-04 era) — adopt or re-derive, do not duplicate.
*   **Lands:** consolidated #26/#46, #32/#50 suites; #51 parser + tests; #36 expanded; #29/#38 intents via pure helpers.

### Cluster S — Storage (`SQLiteFullTextService.swift`, Tier 2)
*   **Symbols:** `ensureColumnExists` (#27/#55 supersession), optionally `insertChunks`/`persistStructuredChunkMetadata` (#39), `deleteContainer` logging (#42).
*   **Direct consumers:** RAGService ingestion/deletion, FTS5 query paths, structured-table retrieval.
*   **Persisted structures:** shared SQLite file with `container_id` isolation; FTS5 tokenization invariants.
*   **Gate:** mandatory storage battery (see STORAGE_MIGRATION_MATRIX) + fixture DB generated from current main BEFORE the change.

### Cluster V — Vector math (`VectorDatabase.swift`, `EmbeddingService.swift`, `GPUComputeService.swift`, Tier-2 `BNNSVectorDatabase.swift`)
*   **Symbols:** `computeNorm`, `cosineSimilarity`, `optimizedCosineSimilarity`, GPU norm helpers, `NLEmbeddingProvider` magnitude (#28).
*   **Consumers:** HybridSearchService → RAGService → every retrieval flow; persisted BNNS norms.
*   **Gate:** benchmark matrix with numerical/rank equivalence (tolerance ≤ 1e-6 rank-stable) vs scalar; device re-run OWNER_ACTION_REQUIRED.

### Cluster M — Model routing / entitlement (Tier-2 `FoundationModelSessionFactory.swift`, `EngineSDKCompatibility.swift`) — MAIN-1/MAIN-2 only
*   **Consumers:** LLMService, RAGService routing, PCC consent flow, App Intents (deadlock risk R07), telemetry.
*   **Gate:** named authorization for both files together; full build matrix re-run; device entitlement behavior OWNER_ACTION_REQUIRED.

### Cluster R — RAG semantics (#53, #59, #62-intent, #63-intent, #64, #68; #56 only if benchmarked)
*   **Files:** `QueryEnhancementService.swift`, `StructuredDocumentParser.swift`, `AgenticOrchestrator.swift`, `HybridSearchService.swift`, `ContextualCompressionService.swift`, Tier-2 `RAGService.swift`.
*   **Consumers:** the entire answer pipeline; SuggestedQuestions UI; ingestion quality metadata.
*   **Gate:** measured RAG golden baseline FIRST (RAG_QUALITY_BASELINE) — otherwise reworks stay unimplemented.

### Cluster G — GPU memory pressure (#67 intent; `GPUComputeService.swift`)
*   **Consumers:** Metal search tiers, buffer pool, vector search under memory warnings.
*   **Gate:** observer-lifecycle tests + device memory-warning validation.

## Analysis of PR #59 (Proposed Change Blast Radius)
If PR #59 were to be implemented, its blast radius would be:
*   **Target File:** `OpenIntelligence/Services/Document/Processing/StructuredDocumentParser.swift`
*   **Target Symbol:** `StructuredDocumentParser.parsePage(...)`
*   **Direct Callers:** `DocumentProcessor`
*   **Blast Radius Components:** Document ingestion pipeline, OCR fallback loops, and chunk quality metadata indexing.
*   **Consequences of Quality Score Changes:** If the quality score is skewed, it directly affects whether the ingestion pipeline runs OCR fallback loops. Over-estimation suppresses OCR fallback on pages that require it (e.g. dense tables), causing text extraction failure. Under-estimation triggers redundant CPU/GPU-heavy OCR fallbacks, degrading device performance.
